name: scan-any-public-image
on:
  workflow_dispatch:
    inputs:
      releaseVersions:
        description: 'Release versions'
        required: true
jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upgrade otel
        run: |
          echo "${{ github.event.inputs.releaseVersions }}"
          bash -x ./script.sh "${{ github.event.inputs.releaseVersions }}"
      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "github-actions@github.com"

      - name: Commit changes
        run: |
          git add .
          git commit -m "Update otelcollector" || echo "No changes to commit"
          git push origin HEAD:${GITHUB_REF}

      # - name: Create Pull Request
      #   id: cpr
      #   uses: peter-evans/create-pull-request@v7
      #   with:
      #     token: ${{ secrets.PAT }}
      #     commit-message: Update report
      #     committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
      #     author: ${{ github.actor }} <${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com>
      #     signoff: false
      #     branch: example-patches
      #     delete-branch: true
      #     title: '[Example] Update report'
      #     body: |
      #       Update report
      #       - Updated with *today's* date
      #       - Auto-generated by [create-pull-request][1]

      #       [1]: https://github.com/peter-evans/create-pull-request
      #     draft: false
